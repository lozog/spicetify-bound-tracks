{
    "name": "bound-tracks",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "spicetify-creator --out=dist --minify",
        "build-local": "spicetify-creator --out=dist",
        "build-and-copy": "npm run build-local && cp dist/bound-tracks.js ~/.spicetify/Extensions",
        "watch": "spicetify-creator --watch"
    },
    "license": "MIT",
    "devDependencies": {
        "@types/react": "^19.0.12",
        "@types/react-dom": "^19.0.4",
        "spicetify-creator": "^1.0.17"
    }
}
